@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
    @Html.AntiForgeryToken()

   <!-- <ul class="nav navbar-nav navbar-right">-->
        <ul class="nav navbar-nav ml-auto">
           
            <li class="nav-link">@{Html.RenderAction("CartSummary", "ReservatieKaarts");}

        </ul>
    }
}
else
{
    <ul class="nav navbar-nav ml-auto">
        <li class="nav-link">@{Html.RenderAction("CartSummary", "ReservatieKaarts");}</li>
        <!--<li>@Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>-->
        <!--<li>@Html.ActionLink("Aanmelden", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>-->
    </ul>
}
