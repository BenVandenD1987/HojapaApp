@model IEnumerable<HojapaApplication.Models.Categorie>
<ul id="categories">
    @foreach (var genre in Model)
    {
        <li>
            @Html.ActionLink(genre.Name,
                     "Browse", "ReservatieTypes",
                     new { Genre = genre.Name }, null)
        </li>
    }
</ul>